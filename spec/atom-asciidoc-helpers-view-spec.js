'use babel';

import AtomAsciidocHelpersView from '../lib/atom-asciidoc-helpers-view';

describe('AtomAsciidocHelpersView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
